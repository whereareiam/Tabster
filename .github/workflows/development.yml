name: Development

on:
  push:
    branches:
      - dev

jobs:
  setup:
    runs-on: ubuntu-latest

    outputs:
      projectName: ${{ steps.project_details.outputs.project_name }}
      projectVersion: ${{ steps.project_details.outputs.project_version }}

    steps:
      - uses: actions/checkout@v4
      - name: Extract project details
        id: project_details
        run: |
          echo "PROJECT_NAME=$(mvn -q -Dexec.executable=echo -Dexec.args='${project.artifactId}' --non-recursive exec:exec)" >> $GITHUB_OUTPUT
          echo "PROJECT_VERSION=$(mvn -q -Dexec.executable=echo -Dexec.args='${project.version}' --non-recursive exec:exec)" >> $GITHUB_OUTPUT

  Velocity:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          server-id: github
          settings-path: ${{ github.workspace }}

      - name: Build Velocity
        run: |
          mvn -pl core,velocity -B -P dev package --file pom.xml
      - name: Move artifact
        env:
          projectName: ${{ needs.setup.outputs.projectName }}
          projectVersion: ${{ needs.setup.outputs.projectVersion }}
        run: |
          mv velocity/target/$projectName-VELOCITY-$projectVersion.jar ${{ github.workspace }}/

  Spigot:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          server-id: github
          settings-path: ${{ github.workspace }}

      - name: Build Spigot
        run: |
          mvn -pl core,spigot -B -P dev package --file pom.xml
      - name: Move artifact
        env:
          projectName: ${{ needs.setup.outputs.projectName }}
          projectVersion: ${{ needs.setup.outputs.projectVersion }}
        run: |
          mv velocity/target/$projectName-SPIGOT-$projectVersion.jar ${{ github.workspace }}/

  Paper:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          server-id: github
          settings-path: ${{ github.workspace }}

      - name: Build Spigot
        run: |
          mvn -pl core,paper -B -P dev package --file pom.xml
      - name: Move artifact
        env:
          projectName: ${{ needs.setup.outputs.projectName }}
          projectVersion: ${{ needs.setup.outputs.projectVersion }}
        run: |
          mv paper/target/$projectName-PAPER-$projectVersion.jar ${{ github.workspace }}/

  uploadArtifacts:
    needs: [ Velocity, Spigot, Paper ]
    runs-on: ubuntu-latest

    steps:
      - name: Upload Artifacts
        uses: actions/upload-artifact@v3
        with:
          name: Tabster-b${{ github.run_number }}
          path: ${{ github.workspace }}/*.jar